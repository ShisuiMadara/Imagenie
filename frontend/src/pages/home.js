import react from "react";

export default class Home extends react.Component {
    constructor(props) {
        super(props);
    }
    render(props) {
        return <>This is homepage.</>;
    }
}
